# Homebrew formulae (macOS). One per line; '#' comments ignored.

# shell + terminal
zsh
tmux

# prompt (default; bullet-train is the opt-in --powerline alternative, fetched
# by modules/10-shell.sh rather than listed here)
starship

# editors
# neovim is NOT here on purpose - Homebrew's version floats onto whatever is
# currently stable, which has already shipped a Neovim 0.12.x that crashes on
# this config (see modules/30-editor.sh). It's installed from a pinned GitHub
# release tarball instead, same as Linux.
vim

# vcs + core cli
git
curl
wget

# search
fzf
the_silver_searcher
ripgrep
# fd is the fast find that snacks.picker's explorer search uses; without it the
# explorer's search box errors with "no search file 'fd'".
fd

# viewing / formatting
jq
tig
bat

# runtime for Claude Code tooling and several language servers
node

# macOS power/thermal monitor for Apple Silicon
asitop
