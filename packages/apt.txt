# Debian/Ubuntu packages. One per line; '#' comments and blank lines ignored.
#
# Deliberately NOT here:
#   fzf     - apt ships 0.29, which predates `fzf --zsh`; modules/40-tools.sh
#             installs it from git instead.
#   neovim  - apt ships 0.6.1 on 22.04, far below what the Lua config needs;
#             modules/30-editor.sh installs a pinned official release.
#   sudo    - must already exist; the test container provides it so that
#             nothing this file is responsible for is pre-provisioned.

# shell + terminal
zsh
tmux
locales

# editors (vim here is the plain fallback editor; neovim is handled separately)
vim

# vcs + core cli
git
curl
wget
unzip
zip
net-tools

# search
silversearcher-ag
ripgrep

# viewing / formatting
jq
tig
bat

# clipboard bridge for tmux/vim over X11
xclip

# desktop notification binary, used by the Claude Code completion hook
libnotify-bin

# toolchain: nvim-treesitter compiles parsers and telescope-fzf-native builds a
# native extension, both at plugin-install time. Without this the editor module
# fails halfway through.
build-essential

# python: used by the Claude Code settings merge and the notification hook
python3
python3-pip
